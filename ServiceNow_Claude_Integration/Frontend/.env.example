# Optional. Where the Vite dev/preview server forwards /api/* requests.
# This is the local MCP HTTP API, NOT ServiceNow. Never put ServiceNow
# credentials in this folder; they live only in servicenow-mcp/.env.
BACKEND_URL=http://127.0.0.1:8095